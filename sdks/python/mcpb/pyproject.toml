[project]
name = "bee-mcpb"
version = "1.0.11"
description = "Bee MCP bundle launcher - runs the hosted bee-mcp server from bee-sdk"
requires-python = ">=3.10"
dependencies = [
    "bee-sdk>=1.0.11",
]
